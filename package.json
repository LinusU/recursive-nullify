{
  "name": "recursive-nullify",
  "version": "1.0.0",
  "license": "MIT",
  "repository": "LinusU/recursive-nullify",
  "scripts": {
    "test": "standard && node test"
  },
  "devDependencies": {
    "standard": "^12.0.1"
  },
  "engines": {
    "node": ">=6"
  }
}
